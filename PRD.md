# PRD: CC Pocket

## 1. 概要

**プロダクト名**: CC Pocket

**実装契約（SDD Spec）**: [`specs/`](./specs/) を参照。

**一言でいうと**: PCで動いているClaude Codeを、スマホからリアルタイムに見て・入力して・承認操作できるリモートコントローラ。

**位置づけ**: 自分用のエンジニアリング生産性ツール。外出先やソファからでもClaude Codeセッションに介入したい。スケール想定なし。自宅サーバ + 個人デバイスでの利用が前提。

## 2. 背景と目的

Claude Codeを使った開発フローでは「AIがツール実行の承認を待っている時間」が頻発する。PCの前にいないと止まり、戻ってきて承認するだけで作業が再開する瞬間が多い。これをスマホから即座に承認・指示できれば、待ち時間がゼロになり、開発体験が大きく変わる。

公式・非公式の既存ツール（Claude Code Web、SSH+tmux、Remote Control等）を試したが、いずれも「PCと同じセッションをスマホで自然に操作する」体験には届いていなかった。

**検証したいこと**:
- 承認待ちをスマホ通知で即座にさばけることで、Claude Codeとの体感連続性が上がるか
- マルチセッション（複数tmuxウィンドウ）の状態色分けが運用に効くか
- 自宅外からでも安心して使える認証・接続方式は成立するか

## 3. 要件

### Must
- Claude Codeの出力がスマホでリアルタイムに見える（ストリーミング）
- スマホからテキスト入力をPC側のセッションに送れる
- ツール実行承認（[y/n]）をネイティブUIボタンで操作できる
- 複数のtmuxウィンドウを切り替えでき、どれが「承認待ち」か一目でわかる
- 外出先（自宅外）から安全に接続できる

### Should
- 承認待ち発生時にスマホへプッシュ通知（バイブ込み）
- PWAでホーム画面追加 → フルスクリーンで使える
- セッション状態のバッジ（実行中／承認待ち／待機）

### Won't（今回はやらない）
- 複数ユーザー対応・ロール管理
- クラウドホスティング型のSaaS化
- iOSネイティブアプリ化（v2以降の検討事項）

## 4. 技術スタック

| 項目 | 選定 |
|------|------|
| Claude Code起動 | tmuxセッション内でCLI実行 |
| バックエンド | Node.js（標準モジュールのみ: http, fs, child_process） |
| 通信 | SSE（出力配信）+ HTTP POST（入力送信） |
| フロントエンド | HTML/CSS/JS 単一ファイル + PWA manifest |
| 外部アクセス | Tailscale（仮想LAN方式） |
| 認証 | PIN認証 + Tailscaleデバイス制限の二段 |
| 開発スタイル | 写経ベースで最小実装 → 不満ベースで改善 |

参考実装: [tokium_dev/cc-remote-claude-code-from-smartphone](https://zenn.dev/tokium_dev/articles/cc-remote-claude-code-from-smartphone)

将来ネイティブ化する場合: SwiftUI（iOS優先）。バックエンドは流用、クライアントのみ作り直し。

## 5. アーキテクチャ

```
┌──────────────┐         ┌────────────────────────┐
│  スマホ(PWA) │  HTTPS  │  自宅Mac               │
│              │ ←─SSE──→│  ┌──────────────────┐  │
│  - 出力表示  │         │  │ Node.jsサーバ    │  │
│  - 入力欄    │  POST   │  │ (1000行)         │  │
│  - 承認ボタン│ ──────→ │  └────────┬─────────┘  │
│  - 状態バッジ│         │           │             │
└──────────────┘         │  ┌────────▼─────────┐  │
   ↑                     │  │ tmux             │  │
   │ Tailscale経由       │  │  ├─ window 0     │  │
   │ (100.x.x.x)         │  │  │   claude code │  │
   └─────────────────────┤  │  ├─ window 1     │  │
                         │  │  │   claude code │  │
                         │  │  └─ window 2     │  │
                         │  │      claude code │  │
                         │  └──────────────────┘  │
                         └────────────────────────┘
```

**通信フロー**:
- 出力: tmux `pipe-pane` → ファイル監視 → SSE → スマホ
- 入力: スマホPOST → `tmux send-keys -t window` → セッション
- 承認検知: 出力末尾を正規表現スキャン → 状態を `awaiting_approval` に遷移 → スマホへ通知

## 6. 主要機能

### 6.1 リアルタイム出力ストリーミング
- tmuxの`pipe-pane`で各ウィンドウの出力をファイルに書き出す
- Node.jsがファイル末尾を監視、差分をSSEでクライアントに送信
- クライアント側はチャットログ風UI（生ターミナルではなく整形表示）

### 6.2 承認プロンプト検知 → ネイティブボタン化
- 出力末尾を正規表現でスキャン
- パターン例: `Do you want to proceed?`, `[y/n]`, 番号選択肢
- 検知時はクライアントに「許可 / 拒否 / 全て許可」の3ボタンをバナー表示
- ボタンタップ → 対応キーストロークをPOST → `tmux send-keys`

### 6.3 マルチウィンドウ管理
- ウィンドウ一覧をタブUIで表示
- 各タブにステータスドット:
  - 🟢 緑: 待機中（プロンプト待ち）
  - 🟡 黄: AI実行中
  - 🔴 赤: 承認待ち
- タブタップで該当ウィンドウの出力に切り替え

### 6.4 入力送信
- 画面下部の入力欄からテキスト送信
- Enter送信 / Shift+Enter改行
- 送信内容は`tmux send-keys`経由でClaude Codeへ

### 6.5 通知
- 承認待ち発生時にWeb Push通知（PWAインストール時のみ）
- iOS 16.4+ / Android対応

## 7. UI設計（ワイヤフレーム）

```
┌─────────────────────────┐
│ CC Pocket   [w0🔴][w1🟡][w2🟢]│ ← ウィンドウタブ（状態色）
├─────────────────────────┤
│                          │
│  > read README.md        │
│                          │
│  Read 53 lines           │
│                          │
│  > use Bash to run tests │
│                          │
│  ⚠️ 承認待ち              │ ← 承認バナー
│  npm test を実行しますか？  │
│  [許可] [拒否] [常に許可]  │
│                          │
├─────────────────────────┤
│ [_____________入力______] [送信]│
└─────────────────────────┘
```

## 8. セキュリティ

### 二段防御
1. **Tailscaleデバイス制限**: 自分のデバイスしかネットワーク到達不可。MAC制限の正攻法版
2. **PIN認証**: 万一デバイス紛失時の保険

### 起動運用
- Node.jsサーバは都度起動（常駐させない）
- tmuxセッションも作業時のみ立ち上げ
- 認証情報はlocalStorageに保存しない（毎回PIN入力）

### やらないこと
- インターネット直公開（NAT越えしない）
- グローバルIPでのlisten
- 第三者へのアクセス共有

## 9. 開発フェーズ

| Phase | 内容 | 期間目安 |
|-------|------|----------|
| Phase 0 | Tailscaleセットアップ（Mac + iPhone） | 30分 |
| Phase 1 | Zenn記事構成の写経 → 自宅WiFiで動作確認 | 半日〜1日 |
| Phase 2 | Tailscale経由で外出先から接続確認 | 30分 |
| Phase 3 | 実運用しながら不満ポイントを記録 | 2週間 |
| Phase 4 | 不満ポイントベースで改善（通知・UI調整等） | 都度 |
| Phase 5（条件付） | iOS Swift版に移行 | 不満が貯まったら |

## 10. 制約・将来の検討事項

### 既知の制約
- Claude Code側の出力フォーマットが変わると承認検知regexが壊れる
  → 早めにテスト化、Claude Codeバージョン固定の運用を検討
- iOSのPWAはバックグラウンドで殺されやすい
  → 通知の信頼性が低い場合はネイティブ化検討
- tmux依存なのでWindows/Linux WSLでの利用は別途検証

### 将来の検討
- ネイティブiOSアプリ化（SwiftUI）
  - キーボード入力体験
  - ハプティクス（承認待ち通知の質感）
  - スワイプジェスチャでウィンドウ切替
  - バックグラウンド・プッシュ通知の信頼性
- 音声入力（指示出しを声でやる）
- 複数PC対応（自宅Mac + ノートMacを切替）

## 11. 参考資料

- [スマホからClaude Codeを操作するツールを作った（tokium_dev / Zenn）](https://zenn.dev/tokium_dev/articles/cc-remote-claude-code-from-smartphone)
- [Tailscale](https://tailscale.com/)
- [Claude Code 公式](https://docs.claude.com/claude-code)
